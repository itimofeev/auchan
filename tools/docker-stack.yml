version: '3'

services:

  db:
    image: postgres:11
    ports:
      - "5432:5432"
    environment:
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - pg_data:/var/lib/postgresql/data/pgdata


volumes:
  pg_data:
    driver: local